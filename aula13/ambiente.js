/*
    var c = 1
    do {
        console.log(`${c}`)
        c++
    } while (c <= 500)
*/

/*
    while (c <= 500) {
        console.log(`${c}`)
        c++
    } 
*/

    var beta = ['Omega', 'Alpha', 'Gama', 'Falcon', 'Delta', 'Charlie']

    for (let C = 0; C < beta.length; C++) {
        const x = beta[C];
        console.log(x)
    }